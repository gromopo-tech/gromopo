import React from 'react';

export default function MainDomainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // This layout is for the main domain (gromopo.com)
  return <>{children}</>;
}
