rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to upload and read order PDFs if they are authenticated and their businessId matches the path
    match /businesses/{businessId}/orders/{orderId} {
      allow read, write: if request.auth != null;
    }
  }
}
